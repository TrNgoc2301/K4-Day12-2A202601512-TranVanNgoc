# Ảnh chụp màn hình bản deploy

Các ảnh cần nộp:

- `dashboard.png`: Render Dashboard hiển thị web service `day12-chat` và Key
  Value `day12-chat-redis` đang hoạt động.
- `healthz.png`: URL public `/healthz` trả JSON có `"status": "ok"`.

Có thể giữ thêm `readyz.png` làm bằng chứng Redis đã kết nối.
